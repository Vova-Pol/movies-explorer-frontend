export const imagesUrl = 'https://api.nomoreparties.co';

export const nothingFoundText = 'Ничего не найдено';
export const serverErrorText =
  'Во время запроса произошла ошибка. Возможно, проблема с соединением или сервер недоступен. Подождите немного и попробуйте ещё раз';

export const emailRegex = new RegExp(/^[a-z0-9]+@[a-z]+.[a-z]{2,3}$/);
export const nameRegex = new RegExp(/^[a-z0-9а-яА-ЯЁё\s]+$/);
export const passwordRegex = new RegExp(/^[A-Za-z0-9\w]+$/);
